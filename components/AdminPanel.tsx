"use client";import { useEffect,useState } from "react";
type User={id:string;username:string;isActive:boolean;roles:{role:{name:string}}[];_count:{surveys:number}};type Role={id:string;name:string;description:string|null;_count:{users:number}};
export function AdminPanel(){const[users,setUsers]=useState<User[]>([]),[roles,setRoles]=useState<Role[]>([]);async function load(){setUsers(await(await fetch("/api/admin/users")).json());setRoles(await(await fetch("/api/admin/role-groups")).json())}useEffect(()=>{load()},[]);async function toggle(u:User){const r=await fetch("/api/admin/users",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:u.id,isActive:!u.isActive})});if(!r.ok)alert((await r.json()).error);load()}return <div className="admin-grid"><section className="card"><div className="panel-pad"><h2>用户</h2><p className="muted">管理账号状态和身份组。</p></div><table className="table"><thead><tr><th>用户名</th><th>身份组</th><th>问卷</th><th>状态</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><strong>{u.username}</strong></td><td>{u.roles.map(r=>r.role.name).join("、")||"普通用户"}</td><td>{u._count.surveys}</td><td><button className="btn" onClick={()=>toggle(u)}>{u.isActive?"启用":"禁用"}</button></td></tr>)}</tbody></table></section><section className="card panel-pad"><h2>身份组</h2>{roles.map(r=><div className="role-row" key={r.id}><div><strong>{r.name}</strong><p>{r.description||"未填写说明"}</p></div><span className="badge">{r._count.users} 人</span></div>)}</section></div>}
