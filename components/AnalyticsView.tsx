"use client";import { useEffect,useState } from "react";
type Data={summary:{views:number;completed:number;completionRate:number;abandonRate:number};days:{date:string;count:number}[];questions:{id:string;title:string;total:number;distribution:Record<string,number>;numeric:{average:number;min:number;max:number}|null;texts:string[]}[]};
export function AnalyticsView({id}:{id:string}){const[data,setData]=useState<Data|null>(null);useEffect(()=>{fetch(`/api/surveys/${id}/analytics`).then(r=>r.json()).then(setData)},[id]);if(!data)return <div className="empty">正在汇总数据…</div>;const max=Math.max(1,...data.days.map(d=>d.count));return <><section className="metric-grid"><Metric label="访问量" value={data.summary.views}/><Metric label="完成答卷" value={data.summary.completed}/><Metric label="完成率" value={`${data.summary.completionRate}%`}/><Metric label="放弃率" value={`${data.summary.abandonRate}%`}/></section><section className="card chart-card"><div className="section-head" style={{margin:0}}><h2>近 7 日提交趋势</h2><span className="muted">实时</span></div><div className="bars">{data.days.map(d=><div style={{flex:1}} key={d.date}><div className="bar-wrap"><div className="bar" title={`${d.count} 份`} style={{height:`${Math.max(3,d.count/max*100)}%`}}/></div><div className="bar-label">{d.date}</div></div>)}</div></section><div className="section-head"><h2>题目分析</h2></div><div className="analytics-grid">{data.questions.map(q=><section className="card panel-pad" key={q.id}><h2>{q.title}</h2><p className="muted">{q.total} 个有效回答</p>{q.numeric&&<div className="number-summary"><b>{q.numeric.average.toFixed(1)}</b><span>平均值 · 范围 {q.numeric.min}–{q.numeric.max}</span></div>}{Object.entries(q.distribution).slice(0,8).map(([name,count])=><div className="dist" key={name}><div><span>{name}</span><b>{count}</b></div><div className="dist-track"><span style={{width:`${q.total?count/q.total*100:0}%`}}/></div></div>)}{q.texts.slice(0,5).map((t,i)=><blockquote key={i}>{t}</blockquote>)}</section>)}</div></>}
function Metric({label,value}:{label:string;value:string|number}){return <div className="metric card"><small>{label}</small><b>{value}</b><div className="trend">实时统计</div></div>}
