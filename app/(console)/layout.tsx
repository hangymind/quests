import Link from "next/link"; import { redirect } from "next/navigation"; import { getSessionUser } from "@/lib/auth"; import { Icons } from "@/components/Icons"; import "./console.css";
export default async function ConsoleLayout({ children }: { children: React.ReactNode }) { const user = await getSessionUser(); if (!user) redirect("/login"); const admin = user.roles.some(r=>r.role.name==="超级管理员"); return <div className="console-shell"><aside className="sidebar"><Link href="/dashboard" className="side-brand"><span className="brand-mark">澄</span><span>澄问</span></Link><nav className="nav"><Link href="/dashboard"><Icons.Home/><span>工作台</span></Link><Link href="/surveys"><Icons.Form/><span>我的问卷</span></Link>{admin && <Link href="/admin"><Icons.Users/><span>用户与权限</span></Link>}<Link href="/surveys"><Icons.Chart/><span>数据统计</span></Link></nav><div className="side-user"><strong>{user.username}</strong><span>{admin ? "超级管理员" : "成员"}</span><form action="/api/auth/logout" method="post"><button className="btn logout"><span>退出登录</span></button></form></div></aside><main className="console-main"><header className="topbar"><div><strong>问卷工作空间</strong><br/><span>私有部署 · 数据自持</span></div><span>{new Intl.DateTimeFormat("zh-CN",{dateStyle:"long"}).format(new Date())}</span></header>{children}</main></div>; }
